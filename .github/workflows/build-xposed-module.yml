name: Build Xposed Module

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:
  release:
    types: [ created ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '11'
        cache: 'gradle'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v2
      
    - name: Setup Gradle Wrapper
      run: |
        cd "Xposed模块获取QToken信息"
        if [ ! -f "./gradle/wrapper/gradle-wrapper.jar" ]; then
          echo "Creating Gradle wrapper..."
          mkdir -p gradle/wrapper
          # 下载gradle-wrapper.jar
          curl -L -o gradle/wrapper/gradle-wrapper.jar https://github.com/gradle/gradle/raw/v7.0.2/gradle/wrapper/gradle-wrapper.jar || {
            echo "Failed to download wrapper jar, trying alternative method..."
            # 如果下载失败，尝试使用系统gradle
            if command -v gradle &> /dev/null; then
              gradle wrapper --gradle-version 7.0.2
            else
              echo "Error: Cannot create gradle wrapper"
              exit 1
            fi
          }
        fi
        chmod +x ./gradlew
      
    - name: Build with Gradle
      run: |
        cd "Xposed模块获取QToken信息"
        ./gradlew assembleDebug --no-daemon --stacktrace
      env:
        ANDROID_HOME: ${{ env.ANDROID_HOME }}
        ANDROID_SDK_ROOT: ${{ env.ANDROID_HOME }}
        
    - name: Check APK exists
      run: |
        APK_PATH="Xposed模块获取QToken信息/app/build/outputs/apk/debug/app-debug.apk"
        if [ -f "$APK_PATH" ]; then
          echo "✅ APK构建成功"
          ls -lh "$APK_PATH"
        else
          echo "❌ APK文件不存在"
          find "Xposed模块获取QToken信息" -name "*.apk" -type f
          exit 1
        fi
        
    - name: Upload APK artifact
      uses: actions/upload-artifact@v3
      with:
        name: qtokenhook-module-apk
        path: Xposed模块获取QToken信息/app/build/outputs/apk/debug/*.apk
        retention-days: 30
        
    - name: Create Release
      if: github.event_name == 'release'
      uses: softprops/action-gh-release@v1
      with:
        files: Xposed模块获取QToken信息/app/build/outputs/apk/debug/*.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
